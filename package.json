{
  "name": "airalo-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:ui-only": "playwright test --grep @ui",
    "test:ui-only:headed": "playwright test --grep @ui --headed",
    "test:api-only": "playwright test --grep @api",
    "test:api-token": "playwright test --grep @api-token",
    "test:api-order": "playwright test --grep @api-order",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.5.0",
    "dotenv": "^16.6.1"
  }
}
